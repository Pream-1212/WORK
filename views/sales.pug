doctype html
html
  head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    title MAYONDO WOOD FACTORY
    // Bootstrap CSS
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css' rel='stylesheet')
    // Your custom CSS
    link(href='/css/main.css' rel='stylesheet')

  body
    // Navbar
    nav.navbar.navbar-expand-lg.bg-body-tertiary
      .container-fluid
        // Brand
        a.navbar-brand(href='/')
          img(src='/images/Download Wooden House Logo design for free.jpg', alt='Company Logo', width='80', height='80')
        // Toggler button
        button.navbar-toggler(type='button', data-bs-toggle='collapse', data-bs-target='#navbarNav', aria-controls='navbarNav', aria-expanded='false', aria-label='Toggle navigation')
          span.navbar-toggler-icon
        // Navbar items
        #navbarNav.collapse.navbar-collapse
          ul.navbar-nav.ms-auto
            li.nav-item
              a.nav-link(href='/') Home
            li.nav-item
              a.nav-link(href='/login') Sign in
            li.nav-item
              a.nav-link(href='/stock') Stock
            li.nav-item
              a.nav-link(href='/Addsale') Sales

    // Page Header
    h1 Our Movements

    main.container
      h1 Sales Information
      p
        i Welcome to our sales page!
        |  Here you will find our latest offers.

      h3 Add New Sale
      .form-card.card.shadow.mx-auto.mb-4(style="max-width:500px")
        form#addSaleForm(action='/Addsale', method='post')
          .mb-2
            label(for="saleDate") Date:
            input#saleDate.form-control(type="date", name="date", required)
          .mb-2
            label(for="customer") Customer Name:
            input#customer.form-control(type="text", placeholder="Customer Name", name="name", required)
          .mb-2
            label.form-label Product Name
            select#productName.form-control(type='text', name='productName', required='')
              option(value='') -- Select Product --
              option(value='Officefurniture') Officefurniture
              option(value='Softwood') Softwood
              option(value='Hardwood') Hardwood
              option(value='Cupboards') Cupboards
              option(value='Beds') Beds
              option(value='Cabinets') Cabinets
              option(value='Poles') Poles
              option(value='Woodchips') Woodchips
              option(value='Diningtables') Diningtables
              option(value='Wardrobes') Wardrobes
          .mb-3
            label.form-label Product Type
            select#productType.form-control(name='productType', required)
              option(value='Wood') Wood
              option(value='Furniture') Furniture
              if stocks.length
                each stock in stocks
                  option(value=stock.productName)= stock.productName
          .mb-2
            label(for="quantity") Quantity:
            input#quantity.form-control(type="number", placeholder="Quantity", name="quantity", required)
          .mb-2
            label(for="unitprice") Unit Price: UGX
            input#price.form-control(type="text", placeholder="Total price", name="unitPrice", required)
          .mb-2
            label.payment Payment Type:
            select.form-control#paymentType(name="payment", required)
              option(value="cheque") Cheque
              option(value="cash") Cash
              option(value="bankoverdraft") Bank Overdraft
              option(value="banktransfer") Bank Transfer
              option(value="paypal") PayPal
          //- Optional Sales Agent
          //- .mb-2
          //-   label(for="agent") Sales Agent:
          //-   input#agent.form-control(type="text", placeholder="Sales Agent", name="agent", required)
          .mb-2
            label(for="delivery") Delivery Method:
            input#delivery.form-control(type="text", placeholder="Delivery Method", name="delivery", required)
          button.btn.btn-success.mt-2(type="submit") Add Sale

      // Info Row
      .row.mt-4
        .col.order-last
          h3
            i Possible payment types
          p.
            Pay card (cheque) at our office. Pay with cash at our office. 
            Pay us on internet via our banking service (bank overdraft) and transfer account to invoice. 
            Via our PayPal.
        .col
          h3 Returns
          p
            | To make returns pass via email: mwfco.org, with subject:
            u
              i Returns
          button#returnsBtn.btn.btn-warning View Returns Policy
        .col.order-first
          h3
            i Customers are highly considered
          p
            | We offer transport to our customers. Customers pay 5% + total price.
            i Some of our products are imported from US, China and Denmark

      // Dashboard Image
      img.img-fluid.mx-auto.d-block(
        src="image.png",
        width="300px",
        height="200px",
        alt="Dashboard Image",
        style="margin-top: 20px"
      )

    // Footer
    footer.bg-dark.text-white.text-center.py-3
      p © 2025 MWF Ltd. All rights reserved.

    // Scripts
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js")
    script(src="../js/stable.js")
